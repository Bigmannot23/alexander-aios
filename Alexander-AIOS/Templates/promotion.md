# Template — Promotion Packet

Required for anything moving from a lesson/review into an accepted system
delta, and *mandatory* (with the additional EntryLens promotion lock) for
anything proposed to cross from AlphaLab into EntryLens. See
`00_System/Promotion-Rules.md`.

```yaml
packet_id:
date:
source_lesson_or_review:    # link
```

**Proposed deterministic predicate / reason-code:**

**Avoid-condition definition** (if applicable):

**Fixture idea** (how this would be tested):

**Proof requirement** (what evidence would confirm this works):

**Spec delta** (exact change to the spec/doctrine, if any):

**Manual-approval checklist:**

- [ ] Source claims already audited (`00_System/Claim-Index.md`)
- [ ] Deterministic — no probability/edge/recommendation language anywhere
- [ ] Fixture/test plan defined
- [ ] Reviewed by a human, not auto-approved
- [ ] Decision logged (`08_Decision-Logs/_index.md`) if accepted

**Only deterministic artifacts cross into EntryLens — never
recommendations, probabilities, or contracts.**
